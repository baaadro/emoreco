this file contains the code and the face detection set for a pre trained model
only use this code if you already have used the trained model version as it saves a pretrained model
this is for faster easier access
